apiVersion: v1
kind: ServiceAccount
metadata:
  name: controller
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: controller-scaler
rules:
- apiGroups: ["apps"]
  resources: ["statefulsets"]
  verbs: ["get", "patch", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: controller-binding
subjects:
- kind: ServiceAccount
  name: controller
roleRef:
  kind: Role
  name: controller-scaler
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  labels:
    app: controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: controller
  template:
    metadata:
      labels:
        app: controller
    spec:
      serviceAccountName: controller
      containers:
      - name: controller
        image: hattmo/controller:{{ .Values.images.controller.tag }}
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        env:
        - name: MATRIX_HOSTNAME
          value: "matrix.{{ .Release.Namespace }}.svc.cluster.local"
        - name: VMWARE_GATEWAY_HOSTNAME
          value: "vmware-gateway"
        - name: NAMESPACE
          value: "{{ .Release.Namespace }}"
        - name: MATRIX_SECRET
          valueFrom:
            secretKeyRef:
              name: matrix-shared-secret
              key: shared_secret
        - name: MATRIX_PASSWORD
          valueFrom:
            secretKeyRef:
              name: creds
              key: matrix-password
        - name: MATRIX_CA_CERT
          value: "/etc/ssl/certs/matrix-ca/ca.crt"
        volumeMounts:
        - name: matrix-ca
          mountPath: /etc/ssl/certs/matrix-ca
          readOnly: true
      volumes:
      - name: matrix-ca
        secret:
          secretName: matrix-ca
          defaultMode: 0644
---
apiVersion: v1
kind: Service
metadata:
  name: controller
spec:
  type: ClusterIP
  selector:
    app: controller
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
